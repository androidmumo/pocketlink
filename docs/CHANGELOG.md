简体中文 | [English](CHANGELOG.en.md)

# 更新记录

## 未发布

- 增加设备消息发送人与时间、北京时间分钟时钟；统一房间创建/加入/邀请操作区，分离注册及房间邀请码，禁止手机页面缩放。增加房间半双工 WSS PCM 对讲开发版、麦权与权限检查、网页麦克风和设备独立对讲页；真机音质与延迟待验收。

- 删除空收信页重复的配网说明，保留底部操作提示；工作台使用自定义房间下拉列表，新邀请码支持加密保存后再次查看/复制，固件版本库补充上传和最近发布时间。迁移 006 的恢复密钥需随 data 目录备份。

- 后台收信、电量读取与自动重连/更新检查不再触发加载或占用交互锁；手动耗时操作显示单行“处理中，请稍后”及末尾旋转字符，防止过期按键作用于新消息。

- 增加设备电量计百分比与低电量颜色提示、后台持续文字加载动画及操作互斥；限制网页消息记录区域高度并支持内部滚动。

- 设备配网支持长按确定退出并恢复原网络；消息改为固定页面，上下键翻页，短消息不移动，回执刷新保留页码。

- 修复登录页多余滚动条和侧栏随页面回弹：固定视口布局，内容区独立滚动；小屏幕表单保持可访问。

- 增加管理员邀请码注册、独立用户设备归属和房间邀请/退出/移除；迁移保留旧数据归管理员。重设计桌面与手机工作台，增加用户和管理员入口、消息发送者与邀请码管理。

- 修复手机已连接设备热点仍被配网页拒绝：正确识别双栈套接字的 IPv4 映射地址，并接受显式默认端口 80；保留热点接口、Host、Origin 和令牌校验。

- 修复设备配网界面中文显示方框：补齐界面用字和 GB2312 常用汉字，增加字库覆盖检查。

- 增加签名 OTA 升级包、设备 A/B 升级及按键确认和启动回滚、网页版本管理、SQLite 迁移 004 与 GitHub 手动签名打包；真机验收仍待完成。

- 增加独立 PocketLink 开发固件：二维码热点配网、HTTPS 绑定、持久化单条收信与回执；加入配置和消息状态主机测试及双固件 CI。真机验收待完成。

- 增加房间成员权限、幂等文字发送、离线队列、逐设备接收/已读回执及 WSS 通道；网页支持消息记录。

- 文档改为中文默认、英文切换，README 增加部署、使用、升级、备份恢复及故障排查教程。

- 建立 PocketLink 单仓库底座，导入固定版本 AI Passport 硬件诊断演示。
- 增加配置校验、SQLite 原子迁移、存活/就绪检查、能力描述及优雅退出。
- 增加协议 v1 解析器、共享向量和主机测试。
- 增加隔离容器配置及经检查的镜像/固件 CI 产物。
- 增加可选管理员登录、一次性设备配对、持久化可撤销凭证和嵌入式设备管理页。
  语音对讲仍待实现。
